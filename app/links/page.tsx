import LinksView from "./LinksView";

export const metadata = {
  title: "Lamberts.lens",
  description:
    "Lightroom presets, gear discounts, and photography from Jake Lambert (@lamberts.lens).",
};

export default function LinksPage() {
  return <LinksView />;
}
