import PresetsView from "./PresetsView";

export const metadata = {
  title: "Jake Lambert — Lightroom Presets",
  description:
    "50 Lightroom presets across 10 cinematic collections, built from the real edits behind @lamberts.lens.",
};

export default function PresetsPage() {
  return <PresetsView />;
}
